import React from 'react';
import {
    TouchableOpacity,
    View,
    Text,
    StyleSheet
} from 'react-native';

const Options = props => {
    // console.log(props.keys);

    return (
        <View>
            <Text> {props.keys} </Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default Options;




