class Types {
    constructor(
        id,
        title,
        likes,
        total
    ) {
        this.id = id;
        this.title = title;
        this.likes = likes
        this.total = total
    }
}

export default Types
