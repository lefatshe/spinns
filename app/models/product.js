class Product {
    constructor(id, title, price, quantity, total, categoryTitle, ownerId) {
        this.id = id;
        this.title = title;
        this.price = price;
        this.quantity = quantity;
        this.total = total;
        this.categoryTitle = categoryTitle;
        this.ownerId = ownerId;
    }
}

export default Product;
