class Order {
    constructor(id, title, keys, thumbnailUrl ) {
        this.id = id;
        this.title = title;
        this.keys = keys;
        this.thumbnailUrl = thumbnailUrl
    }
}

export default Order
