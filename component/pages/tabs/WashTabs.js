import React from 'react';
import {
    FlatList,
    StyleSheet,
    View, Text, Picker,
} from 'react-native';


const WashTabs = props => {
    return <View>
        <Text>Hi</Text>
    </View>;
};

const styles = StyleSheet.create({
});

export default WashTabs;
