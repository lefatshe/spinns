class Washes {
    constructor(
        id,
        categoryIds,
        title,
        unit,
        imageUrl,
        type,
        minVal,
        maxVal
    ) {
        this.id = id;
        this.categoryIds = categoryIds;
        this.title = title;
        this.unit = unit;
        this.imageUrl = imageUrl,
        this.minVal = minVal,
        this.maxVal = maxVal
    }
}

export default Washes
